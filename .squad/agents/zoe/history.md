- **2026-05-04T00:00:00Z**: Set up the project structure based on user request. Created infra, src, docs, and test directories, recorded decision.

## Learnings

- **2026-05-05T01:45:46.172+00:00**: Reviewed Wash's `pulp-container-deployment` skill against River's 47-point checklist. Verdict: REVISE. Pattern: deployment skills must cover operational failure paths, not only happy-path commands; permissions, port/name conflicts, FUSE, upgrades, disk sizing, secret verification, and evidence package requirements are review gates for executable AirGap deployment guidance.
- **2026-05-04**: User directive redirected Phase 1 away from Azure IaC toward application architecture. `src/bundle-tools/` is empty — no application code exists yet despite Phase 0 completing the local harness proof. The gap between "bash scripts that prove the workflow" and "deployable application" is the entire Phase 1 now.
- **2026-05-04**: Critical blockers: `run-low-high-e2e.sh` hardcodes `podman` (~8 bare calls) even though `start-single-container.sh` already respects `${PULP_CONTAINER_RUNTIME}`. No `.env` file exists. Dev machine has Docker 29.3.1, no podman.
- **2026-05-04**: Application layers identified: Pulp Client SDK (typed Python wrapper), Manifest Tooling, State Store (PostgreSQL), Orchestration Engine (state machine), Audit System (append-only), CLI (operator interface). Named the CLI `hydra`.
- **2026-05-04**: Readiness gate defined: 7 measurable criteria that must ALL pass before Azure IaC work resumes. Key: "no bash in the critical path" — the application must own the workflow, not shell scripts.
- **2026-05-04**: Harness uses Pulp 3.x single-container image with `pulp_deb`. Full 13-step e2e is proven in bash. Python must replicate this end-to-end.
- **2026-05-04**: `common.sh` defaults: `PULP_CONTAINER_RUNTIME=podman`, `PULP_PULL_POLICY=never`. Dev .env needs `docker` and `missing` respectively.
- **2026-05-04**: USER DIRECTIVE — No Python wrapper around Pulp's API. Pulp has `pulp-cli` and a REST API; wrapping it is redundant maintenance overhead. The `run-low-high-e2e.sh` bash script already proves every Pulp API call we need (sync, export, import-check, import, publish) works with plain `curl`. Our application code should ONLY cover what Pulp doesn't do: transfer manifests (generation + validation), state tracking across the air gap, evidence collection, and scheduling.
- **2026-05-04**: Revised architecture: thin orchestration shell around `pulp-cli`/REST API. No PostgreSQL needed for state (SQLite or JSON files suffice). No state machine engine (linear script with checkpoints). Milestones cut from 7 (M0–M6) to 6 (M0–M5). Removed entire `PulpClient` SDK wrapper milestone. One-way transfers enforced: low exports, high imports, no feedback loop.
- **2026-05-04**: The real value-add areas where Pulp is silent: (1) manifest tooling — batch JSON descriptor with checksums for the physical media transfer, (2) manifest validation on high side before import, (3) state tracking — what batches exist across independent low/high systems, (4) evidence generation for compliance, (5) scheduling sync+export on interval, (6) operator CLI (`hydra`) that abstracts the multi-step workflow.
- **2026-05-04**: MAJOR SCOPE REDUCTION after reading Pulp's source code. Pulp's PulpExport model has `output_file_info` (filename→SHA256 dict) and `toc_info` (TOC filename + SHA256). Pulp's TOC file has `meta.global_hash` + per-chunk checksums. Import task auto-verifies ALL checksums. `import-check` endpoint does sync pre-validation. PulpExporter tracks `last_export` + full export history via API. PulpImporter tracks full import history. Incremental exports built-in (`full=false`). Chunking built-in (`chunk_size`). Net effect: custom checksum generation/validation removed (Pulp does it), SQLite/JSON state store removed (Pulp's API IS the state store), manifest slimmed to thin batch metadata only (60-line script, not a component). Milestones cut from M0-M5 to M0-M4. The actual application is: scheduling, batch metadata manifest, operator CLI formatting, and evidence formalization. That's it.
- **2026-05-04T21:08:34Z**: Phase 0-2 local dev backlog review completed. Identified 7 Phase 0 Docker-compat tasks (all harness scripts hardcode `podman`), 8 Phase 1 hydra CLI tasks, and 4 Phase 2 placeholders. Gate: Phase 0.6 (full harness pass on Docker) must clear before Phase 1 app work starts. Decision written to `.squad/decisions/inbox/zoe-phase-0-2-local-dev.md`. Docker 29.3.1 confirmed available; podman confirmed absent. `docker network create --internal` appears equivalent to podman's version but needs Wash to validate.
- **2026-05-04T21:08:34Z**: **Phase 0 Docker-local burndown APPROVED.** All harness scripts now use runtime auto-detect (no bare `podman`); fixture generation uses pure Python (no host `ar`); `run-low-high-e2e.sh` handles Docker vs Podman network semantics correctly. Only blocker for full execution is real private image references in `.env`. Next actionable: README doc polish, Phase 1 hydra scaffolding, CI syntax-check gate.
- **2026-05-04T21:08:34Z**: **Consolidation complete.** All team decisions merged into decisions.md (13 files, 12016 bytes). Orchestration log and session log written. Phase 0-2 local dev backlog now the team's consolidated record. Team ready for Phase 1 application layer scaffolding (hydra CLI). Phase 2 acceptance criteria deferred.
- **2026-05-04T21:42:09Z**: **Phase 1 Application-First Breakdown completed.** Reconciled OpenSpec Phase 1 files (issues #8–#14) with app-first directive. Split into Track A (application layer, 8 items, actionable now) and Track B (Azure platform, 7 items, deferred until G3 gate). Two items blocked on private image refs. Three reviewer gates defined. Decision written to `.squad/decisions/inbox/zoe-phase1-app-first-breakdown.md`. Key path: `src/hydra/` is the application root; Kaylee owns P1-A1 scaffold.
- **2026-05-04T21:42:09Z**: **Phase 1 Gate G1 APPROVED.** First application-layer batch reviewed and passed. Wash delivered stdlib Python foundation (config, manifest, evidence, workflow plan, CLI). River delivered image-free validation (env/image rules, one-way constraints, harness gates). Book delivered operator runbook. 14 tests all green. No Pulp wrapper, no Azure infra, no secrets in source. Two items remain blocked on private image refs. Next actionable: P1-A2 operator CLI expansion to wire workflow plan into executable commands against running Pulp harness.
- **2026-05-05T01:26:57.473+00:00**: Phase 1 completion review confirms only Gate G1 / first application-first slice should be called done. Azure deployment remains deferred until G3, P1-A2 remains the next executable operator CLI work, and private/internal image references remain external configuration blockers for full container e2e.
- **2026-05-05T01:40:36.497+00:00**: P1-A2 execution contract approved for implementation. Minimum slice is one low-side `bundle_tools execute-export` command that validates the existing plan, calls native Pulp sync/export against an already-running local harness, writes low-side evidence, and returns Pulp export task/TOC/output-file info. No Azure, no custom Pulp wrapper, no manifest generation, no high-side import/publish, no high-to-low feedback, and no G2/G3 claim from this slice alone.
- **2026-05-05T01:42:22.430+00:00**: Phase 1 scope review of issues #8–#14 completed. Verdict: directionally strong for Azure/Pulp platform proof across low side and high side, but not executable as-is until OpenSpec Phase 1 is reconciled with app-first/G3 sequencing and the lean-state decision that Pulp remains the system of record. Required improvements: make low/high parity diff evidence mandatory, make evidence package layout mandatory, add explicit dependency links among #8–#13, and prove target Azure Government/AirGap SKUs early.

- **2026-05-05T01:42:22Z**: Scribe merged decision inbox items related to Zoe; decisions.md updated.

- **2026-05-05T01:40:36.497+00:00**: **P1-A2 lead gate APPROVED WITH SCOPE LIMITS.** `bundle_tools execute-export` is complete only for the narrow low-side image-free sync/export slice. Validation passed (34 unit tests, harness-static, diff check), River accepted validation, Simon accepted controls, and docs avoid G2/G3 overclaiming. G2 remains blocked until P1-A2 through P1-A6 pass together against the local harness; next actionable item is P1-A3 transfer manifest generation.

- **2026-05-05T01:42:22Z**: Scribe merged decision inbox items related to Zoe; decisions.md updated.

- **2026-05-05T01:42:22Z**: Scribe merged decision inbox items related to Zoe; decisions.md updated.

- **2026-05-05T00:00:00Z**: Enforced global rule that all tests must capture evidence using Playwright CLI and log it into the `evidence/` folder with a clear description. Updated River's charter and Copilot instructions to reflect this architecture decision.

## Learnings
* **2026-05-05**: Updated team charter/instructions to enforce Playwright evidence requirements.
